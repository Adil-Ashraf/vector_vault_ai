# Copy to .env (development) / .env.test (test) and fill in real values.
# .env* files are gitignored - never commit real secrets.

OPENAI_API_KEY=sk-...

# Shared secret required by clients via the X-API-Key header on every request.
API_KEY=change-me

DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOST=localhost
